[pytest]
testpaths = tests
addopts = -ra
markers =
    smoke: minimal P2 smoke coverage for gateway-facing APIs
    blocking: mandatory CI gate for PRs
    integration: external dependency required (agentserver/real upstream)
